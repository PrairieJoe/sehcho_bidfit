# 나라장터 입찰공고 주제 적합도 분석·알림 웹 앱 PRD

> 문서 상태: Draft v1.0  
> 작성일: 2026-08-25  
> 기준 시간대: Asia/Seoul (KST)  
> 제품 가칭: **BidFit**  
> 대상 릴리스: MVP

## 1. 문서 목적

본 문서는 조달청 나라장터의 입찰공고를 매일 수집하고, 공고 및 첨부문서를 분석하여 사용자가 설정한 주제와의 적합도를 건별로 산출·통지하는 웹 기반 앱의 제품 요구사항을 정의한다.

이 문서는 제품, 디자인, 백엔드, 프론트엔드, 데이터/AI, QA 및 운영 담당자가 MVP 범위와 완료 조건을 동일하게 이해하도록 하는 기준 문서다.

## 2. 배경과 문제 정의

나라장터에는 매일 다수의 물품·용역·공사·외자 입찰공고가 등록된다. 사용자는 자신의 사업 분야와 관련된 공고를 찾기 위해 공고명, 기관, 예산, 참가 자격 및 공고서·제안요청서·과업지시서 등의 첨부문서를 반복적으로 확인해야 한다.

단순 키워드 검색에는 다음 한계가 있다.

- 공고명에는 핵심 사업 내용이 드러나지 않고 첨부문서에만 상세 과업이 기재되는 경우가 있다.
- 같은 표현이라도 문맥에 따라 관련성이 크게 다르며, 동의어·약어·유사 개념 때문에 관련 공고가 누락될 수 있다.
- 지역, 예산, 면허, 업종, 마감일 등 실제 참여 가능성을 함께 판단해야 한다.
- 정정공고와 재공고를 놓치거나 동일 건을 중복 검토하기 쉽다.
- 매일 반복되는 탐색과 문서 검토에 많은 시간이 든다.

BidFit은 이 과정을 자동화하여 사용자가 “왜 이 공고가 내 주제에 맞는지”를 짧은 시간 안에 판단하도록 돕는다.

## 3. 제품 비전

사용자가 관심 주제와 참여 조건을 한 번 설정하면, 매일 오전 9시에 전일까지 새로 등록되거나 변경된 나라장터 입찰공고 중 적합한 건을 근거와 함께 우선순위화하여 알려주는 업무 기회 탐색 도구를 제공한다.

## 4. 목표와 비목표

### 4.1 목표

1. 매일 전일 00:00:00~23:59:59 KST의 신규·변경 공고를 빠짐없이 수집한다.
2. 공고 메타데이터와 다운로드 가능한 첨부문서의 내용을 통합 분석한다.
3. 사용자별 관심 주제 및 필수/제외 조건을 반영해 0~100점 적합도를 산출한다.
4. 모든 점수에 근거, 감점 요인, 출처 문서 및 분석 신뢰도를 제공한다.
5. 사용자가 설정한 기준 이상인 공고를 건별로 웹 알림 및 이메일에서 확인할 수 있게 한다.
6. 정정·재공고·분석 실패를 추적하고 재처리할 수 있게 한다.
7. API 키, 원문 파일 및 사용자 설정을 안전하게 관리한다.

### 4.2 비목표(MVP)

- 나라장터 로그인, 전자입찰, 투찰, 공동수급사 모집 또는 계약 체결
- 낙찰 가능성이나 투찰 가격의 예측
- 법률·회계·입찰 참가 자격에 대한 확정적 판단
- 모든 전자조달 사이트 또는 민간 입찰 사이트 통합
- 카카오톡, 문자, Slack 등 다중 외부 채널 알림
- 과거 전체 공고의 일괄 백필 및 과거 낙찰 데이터 기반 추천
- 생성형 AI가 입찰 제안서나 제출 서류를 자동 작성하는 기능

## 5. 핵심 가정 및 결정 사항

| 항목 | MVP 결정 |
|---|---|
| 기본 사용자 | 복수 사용자를 지원하되, 소규모 조직/개인 실무자 중심 |
| 수집 대상 | 나라장터 물품·용역·공사·외자 입찰공고 |
| 배치 기준 | 매일 09:00 KST 실행, 전일 등록·변경분 분석 |
| 알림 방식 | 웹 알림 센터 + 이메일 요약. 이메일 안에서 공고별 결과를 구분해 제공 |
| 분석 단위 | `입찰공고번호 + 공고차수`를 하나의 버전으로 관리 |
| 점수 | 주제 적합도 0~100과 참여조건 상태를 분리 표시 |
| AI 방식 | 규칙 기반 필터 + 검색/임베딩 + LLM 근거 분석의 하이브리드 |
| 설명 원칙 | 점수만 제공하지 않고 근거 문장, 감점 이유, 출처 파일을 함께 제공 |
| 원문 보존 | 다운로드 원본, 해시, 추출 텍스트 및 분석 버전을 보존 |
| 실패 원칙 | 문서 분석 실패를 0점으로 처리하지 않고 `부분 분석`으로 표시 |

## 6. 사용자와 주요 사용 시나리오

### 6.1 핵심 사용자

**사업개발/영업 담당자**

- 자사 역량에 맞는 신규 사업 기회를 매일 찾는다.
- 관련 공고의 예산, 일정, 기관, 과업과 참가 조건을 빠르게 파악하고 싶다.
- 관련성이 높은 공고부터 검토하고 내부에 공유해야 한다.

**입찰/제안 담당자**

- 첨부 공고서와 제안요청서에서 과업 범위 및 자격 조건을 확인한다.
- 정정공고나 마감일 변경을 놓치지 않아야 한다.
- 추천 결과의 근거와 원문을 확인해 참여 여부를 결정한다.

**조직 관리자**

- 조직의 관심 주제와 제외 조건을 관리한다.
- 수집·분석·알림 성공 여부와 비용을 모니터링한다.

### 6.2 대표 사용자 스토리

- 사용자는 “생성형 AI 기반 공공기관 업무 자동화”처럼 자연어로 관심 주제를 등록할 수 있다.
- 사용자는 반드시 포함할 개념, 제외할 개념, 대상 업무 구분, 지역, 예산 범위, 기관 및 남은 마감일을 설정할 수 있다.
- 사용자는 매일 오전 9시 이후 관련 공고를 점수순으로 확인할 수 있다.
- 사용자는 각 공고가 추천된 이유와 주제별 근거 문장을 원문 출처와 함께 확인할 수 있다.
- 사용자는 분석 결과가 부정확하면 `관련 있음/없음` 피드백과 사유를 남길 수 있다.
- 사용자는 정정된 공고에서 무엇이 달라졌는지 확인할 수 있다.

## 7. 사용자 경험 및 정보 구조

### 7.1 핵심 화면

1. **로그인/계정**
2. **오늘의 공고 대시보드**
3. **공고 목록 및 검색/필터**
4. **공고 상세 및 분석 근거**
5. **관심 주제 관리**
6. **알림 설정 및 알림 센터**
7. **관리자 운영 현황**

### 7.2 최초 설정 흐름

1. 회원가입 또는 초대 수락
2. 관심 주제 이름 입력
3. 주제 설명 및 우리 회사가 제공할 수 있는 역량 입력
4. 포함 키워드·동의어·제외 키워드 입력
5. 업무 구분, 기관, 지역, 예산, 참가 조건, 최소 잔여일 설정
6. 알림 최소 점수와 이메일 수신 여부 설정
7. 예시 공고 3~5건으로 예상 매칭 결과 미리보기
8. 주제 저장 및 다음 배치부터 활성화

### 7.3 오늘의 공고 카드 필수 정보

- 적합도 점수, 등급 및 분석 신뢰도
- 공고명, 공고번호/차수, 신규·정정·재공고 상태
- 수요기관/공고기관, 업무 구분
- 추정가격 또는 배정예산, 입찰 마감일, 남은 시간
- 2~3문장 요약
- 핵심 적합 근거 최대 3개
- 주요 감점/주의 요인 최대 3개
- 참가조건 상태: `충족 가능`, `확인 필요`, `조건 불일치`
- 첨부문서 분석 상태와 분석한 파일 수
- 원문 상세, 첨부 다운로드, 관심/보관, 관련성 피드백 액션

### 7.4 공고 상세 화면

공고 상세 화면은 다음 섹션을 제공한다.

1. 공고 기본정보
2. AI 요약
3. 주제 적합도 점수 구성
4. 근거 문장과 문서 출처
5. 과업 범위 및 주요 산출물
6. 참가 자격·지역·면허·업종·공동수급 조건
7. 일정, 예산 및 계약 방식
8. 첨부파일 목록과 파일별 처리 상태
9. 변경 이력 및 이전 버전과의 차이
10. 사용자 메모, 상태 및 피드백

## 8. 기능 요구사항

### FR-01. 인증 및 권한

- 이메일 기반 로그인과 비밀번호 재설정을 지원한다.
- 역할은 `사용자`와 `관리자`로 구분한다.
- 사용자는 자신 또는 소속 조직의 주제, 분석 결과, 메모만 열람할 수 있다.
- 관리자는 API/배치 상태, 실패 건, 재처리 및 비용 현황을 열람할 수 있다.
- 모든 관리자 작업은 감사 로그에 남긴다.

### FR-02. 관심 주제 관리

사용자는 주제를 생성·수정·비활성화·복제할 수 있다. 주제는 다음 필드를 가진다.

| 필드 | 필수 | 설명 |
|---|---:|---|
| 주제명 | Y | 목록과 알림에 표시할 짧은 이름 |
| 주제 설명 | Y | 원하는 사업/과업을 자연어로 설명 |
| 보유 역량 | N | 자사 제품, 기술, 레퍼런스 및 수행 가능 범위 |
| 필수 개념 | N | 적어도 하나 또는 모두 포함할 개념 |
| 가점 개념 | N | 있으면 점수를 높이는 개념과 동의어 |
| 제외 개념 | N | 포함 시 제외하거나 감점할 개념 |
| 대상 업무 | Y | 물품/용역/공사/외자, 복수 선택 |
| 기관 필터 | N | 포함·제외 기관 |
| 지역 필터 | N | 수행 지역 또는 참가 가능 지역 |
| 예산 범위 | N | 최소·최대 금액 |
| 필수 자격 | N | 면허, 업종, 인증 등 사용자가 보유한 조건 |
| 최소 잔여일 | N | 분석 시점부터 마감까지 필요한 최소 일수 |
| 알림 기준 점수 | Y | 기본값 70점 |
| 강제 제외 규칙 | N | 제외 키워드/조건 충족 시 알림 제외 |

- 저장 시 필수/제외 조건 간 충돌을 검증한다.
- 주제 변경은 다음 분석부터 적용하며, 사용자가 요청하면 최근 30일을 재분석할 수 있다.
- 분석 결과에는 적용된 `topic_version`을 기록하여 재현 가능하게 한다.

### FR-03. 나라장터 OpenAPI 연동

- 공공데이터포털의 `조달청_나라장터 입찰공고정보서비스`를 사용한다.
- Base URL은 현재 공개 명세의 `https://apis.data.go.kr/1230000/ad/BidPublicInfoService`를 기준으로 하되 환경설정으로 관리한다.
- 인증키는 서버의 비밀 저장소에만 보관하며 브라우저, 로그, 오류 메시지에 노출하지 않는다.
- 물품·용역·공사·외자별 오퍼레이션을 각각 호출하고 내부 공통 스키마로 정규화한다.
- `공고게시일시 기준`으로 조회하며 시작/종료 시각, 페이지 번호 및 페이지 크기를 명시한다.
- API 페이지네이션을 끝까지 순회하며 페이지별 원시 응답과 수집 결과를 추적한다.
- 응답 결과코드가 성공이 아니거나 스키마가 달라지면 실패로 기록하고 경고한다.
- 429, 5xx, 타임아웃은 지수 백오프와 지터를 적용해 재시도한다.
- 일일 호출량은 개발계정 기본 한도를 고려하고, 운영 전 실제 예상 호출량에 맞춰 운영계정 트래픽 증설을 신청한다.

### FR-04. 배치 스케줄링 및 수집 범위

- 매일 **09:00 KST**에 자동 배치를 시작한다.
- 기본 수집 구간은 전일 00:00:00~23:59:59 KST이다.
- API 지연 등록을 보완하기 위해 최근 72시간을 겹쳐 조회하고 멱등 upsert로 중복을 제거한다.
- 신규 공고뿐 아니라 정정/변경/재공고 상태를 확인한다.
- 배치 시작·종료 시각, 조회 구간, 유형별 수집 수, 신규/변경/중복/실패 수를 기록한다.
- 동일 수집 구간을 수동 재실행해도 공고나 알림이 중복 생성되지 않아야 한다.
- 09:00 실행 실패 시 09:15, 09:45, 10:45에 자동 재시도한다.
- 최종 실패 시 관리자에게 즉시 운영 경고를 보내고, 성공 전까지 사용자에게 완료 알림을 발송하지 않는다.
- 관리자는 날짜 범위를 지정해 재수집할 수 있다.

### FR-05. 중복·버전·변경 관리

- 논리 공고 키는 `입찰공고번호`, 버전 키는 `입찰공고번호 + 공고차수`로 한다.
- 원시 응답의 정규화된 해시가 달라지면 동일 차수라도 변경 후보로 기록한다.
- 정정/재공고 여부 및 변경이력 API를 이용해 상태를 보강한다.
- 첨부 URL, 파일명, 일정, 예산, 참가 조건이 바뀌면 재다운로드·재분석한다.
- 이전 알림을 삭제하지 않고 `변경됨` 배지를 추가하며, 중요 변경을 별도 통지한다.
- 중요 변경은 마감일, 예산, 참가 자격, 과업 범위, 첨부문서의 변경을 포함한다.

### FR-06. 공고 및 첨부파일 다운로드

- API가 제공하는 공고상세 문서, 보충설명 문서, 표준공고 문서 URL과 파일명을 수집한다.
- 첨부파일은 서버가 HTTPS로 직접 다운로드하며 사용자 브라우저가 대신 호출하지 않는다.
- 파일당 최대 크기, 공고당 최대 파일 수, 전체 배치 다운로드 한도를 설정값으로 관리한다.
- 허용 확장자는 기본적으로 PDF, HWP, HWPX, DOC, DOCX, XLS, XLSX, PPT, PPTX, TXT, ZIP이다.
- 실제 MIME 유형, 확장자, 매직바이트가 일치하는지 검사한다.
- ZIP은 격리된 환경에서 압축 폭탄, 경로 이탈 및 중첩 깊이를 검사한 후 해제한다.
- 파일별 SHA-256 해시로 중복 저장과 불필요한 재분석을 방지한다.
- 원본 파일명, 안전한 저장 키, URL, 다운로드 시각, 크기, MIME, 해시, 결과 상태를 기록한다.
- 다운로드 URL 만료 또는 인증/세션 요구 등으로 파일을 받지 못하면 `다운로드 실패`로 표시하고 재시도한다.
- 첨부가 없거나 일부만 실패해도 공고 메타데이터 분석은 계속한다.

### FR-07. 문서 텍스트 추출 및 OCR

- PDF, HWP/HWPX, DOC/DOCX, XLS/XLSX, PPT/PPTX, TXT에서 텍스트를 추출한다.
- 텍스트 레이어가 없는 스캔 PDF와 이미지에는 한국어 OCR을 적용한다.
- 표는 가능한 경우 행/열 구조와 페이지 또는 시트 위치를 보존한다.
- 머리말·꼬리말·페이지 번호의 반복 노이즈를 제거한다.
- 파일별 추출 상태는 `성공`, `부분 성공`, `OCR 성공`, `지원 안 됨`, `실패`로 기록한다.
- 페이지/문단/시트 단위 출처 위치를 유지하여 분석 근거에서 원문을 찾을 수 있게 한다.
- 비밀번호 보호, 손상, DRM 또는 지원하지 않는 포맷은 억지로 처리하지 않고 사유를 노출한다.
- LLM 입력 전에 주민등록번호, 계좌번호 등 불필요한 고위험 개인정보 패턴을 마스킹한다.

### FR-08. 분석 대상 사전 필터

비용과 처리시간을 제어하기 위해 두 단계로 처리한다.

1. **저비용 후보 선별:** 공고명, 분류명, 기관, 품명, 파일명, 구조화 필드를 대상으로 포함/제외 규칙과 전문 검색을 수행한다.
2. **정밀 분석:** 후보 공고의 첨부문서 및 구조화 데이터를 임베딩/LLM으로 분석한다.

- 강제 제외 규칙은 사용자에게 명시적으로 보여야 한다.
- 사전 필터 탈락 건도 검색 화면에서 볼 수 있으며 탈락 사유를 제공한다.
- 단순 키워드 미일치만으로 의미상 관련 공고를 제거하지 않도록 의미 검색 후보를 병행한다.
- 여러 주제가 동일 공고를 분석할 경우 문서 추출·요약 결과를 재사용하고 주제별 점수만 별도로 계산한다.

### FR-09. 공고 내용 분석

정밀 분석은 다음 구조화 결과를 생성한다.

- 한 줄 요약 및 2~5문장 상세 요약
- 발주 목적과 배경
- 주요 과업 범위
- 요구 기술/제품/서비스
- 주요 산출물과 납품 조건
- 사업 기간 및 주요 일정
- 예산/추정가격/배정예산과 부가세 기준
- 계약 및 낙찰 방식
- 참가 자격, 면허/업종, 지역 제한, 공동수급 조건
- 필수 실적·인력·인증 조건
- 제안서 평가 요소(문서에 있을 경우)
- 위험/주의사항 및 확인이 필요한 누락 정보
- 주제별 적합/부적합 근거와 출처

- 구조화 API 필드와 문서 내용이 충돌하면 둘 다 보존하고 `정보 불일치`로 표시한다.
- 분석 모델명, 프롬프트 버전, 입력 문서 해시, 분석 시각을 저장한다.
- LLM 출력은 JSON 스키마 검증을 통과해야 하며 실패 시 자동 수정 1회 후 재처리 큐로 보낸다.
- 문서에 없는 사실은 추론으로 단정하지 않고 `확인 필요`로 표시한다.

### FR-10. 주제 적합도 점수화

#### 10.1 점수 원칙

- 적합도는 **주제와 과업 내용의 부합 정도**를 나타내는 0~100점이다.
- 참여 자격 충족 여부는 적합도와 별도 상태로 표시하여, 좋은 사업이지만 자격이 맞지 않는 경우를 숨기지 않는다.
- 점수는 결정론적 계산식과 버전 관리된 AI 하위 점수를 결합한다.
- 모든 점수에는 계산 내역과 텍스트 근거가 있어야 한다.

#### 10.2 기본 가중치

| 평가 요소 | 가중치 | 판단 내용 |
|---|---:|---|
| 핵심 과업/목적 의미 유사도 | 35 | 주제 설명과 실제 과업의 의미적 일치 |
| 요구 역량/기술 부합도 | 20 | 사용자 보유 역량과 요구 기술·서비스 일치 |
| 필수 개념 및 가점 개념 | 15 | 키워드가 아닌 문맥을 포함한 개념 충족 |
| 사업 유형/산출물 부합도 | 10 | 원하는 용역·제품 및 산출물 형태와 일치 |
| 기관/산업/지역 선호도 | 5 | 사용자가 설정한 선호 조건 일치 |
| 예산 적합도 | 5 | 설정한 수행 가능 금액 범위와 일치 |
| 일정/마감 실행 가능성 | 5 | 최소 잔여일 및 사업 기간 적정성 |
| 근거 충분성 | 5 | 첨부 분석 범위와 명확한 근거의 충분성 |
| **합계** | **100** |  |

관리자가 기본 가중치를 정하고, 사용자는 고급 설정에서 합계 100 범위 내 조정할 수 있다.

#### 10.3 감점 및 강제 규칙

- 제외 개념이 핵심 과업에 해당: 기본 -20~-60점 또는 사용자가 설정한 경우 강제 제외
- 마감 경과: 알림 제외, 목록에는 `마감` 표시
- 최소 잔여일 미달: 일정 점수 0점 및 `검토시간 부족` 경고
- 예산 범위 벗어남: 예산 점수 감점하되 기본적으로 강제 제외하지 않음
- 명백한 대상 업무 불일치: 강제 제외
- 사용자 보유 자격과 면허/지역 제한이 명백히 불일치: 적합도는 유지하고 참가조건을 `조건 불일치`로 표시
- 문서 다운로드/추출 실패: 관련성 자체를 감점하지 않고 근거 충분성과 신뢰도만 낮춤

#### 10.4 등급과 신뢰도

| 점수 | 등급 | 기본 의미 |
|---:|---|---|
| 85~100 | 매우 높음 | 우선 검토 권장 |
| 70~84 | 높음 | 검토 권장 |
| 50~69 | 보통 | 조건부 검토 |
| 0~49 | 낮음 | 기본 알림 제외 |

신뢰도는 `높음`, `보통`, `낮음`으로 별도 표시한다.

- 높음: 핵심 첨부 분석 성공, 다수의 직접 근거 존재, 정보 충돌 없음
- 보통: 일부 첨부 누락 또는 구조화 필드 중심 분석
- 낮음: 첨부 분석 실패, OCR 품질 저하, 근거 부족 또는 정보 충돌

#### 10.5 점수 응답 예시

```json
{
  "score": 87,
  "grade": "매우 높음",
  "confidence": "높음",
  "eligibilityStatus": "확인 필요",
  "components": [
    {"name": "핵심 과업/목적", "score": 32, "maxScore": 35},
    {"name": "요구 역량/기술", "score": 18, "maxScore": 20}
  ],
  "positiveReasons": [
    {
      "reason": "생성형 AI 기반 내부 업무 자동화 구축이 핵심 과업임",
      "sourceFile": "제안요청서.pdf",
      "sourceLocation": "p.12"
    }
  ],
  "penalties": ["사업 기간이 사용자의 선호 기간보다 1개월 짧음"],
  "uncertainties": ["직접생산확인증명서 보유 여부 확인 필요"]
}
```

### FR-11. 알림

- 주제별 점수가 알림 기준 이상이고 강제 제외되지 않은 공고에 알림을 생성한다.
- 웹 알림은 분석 완료 즉시 표시한다.
- 이메일은 해당 일자 배치가 완료된 후 사용자별로 한 번 발송하되, 본문에 공고별 카드와 상세 링크를 제공한다.
- 결과가 0건이어도 사용자가 선택하면 `오늘 신규 추천 없음` 이메일을 보낸다.
- 정정공고의 중요 변경은 기존 점수가 기준 미만으로 내려가더라도 별도 알림할 수 있다.
- 동일 `사용자 + 주제 + 공고버전 + 알림유형`은 한 번만 발송한다.
- 이메일에는 공고별 점수, 요약, 근거, 기관, 예산, 마감일, 참가조건 상태 및 상세 링크를 포함한다.
- 사용자는 이메일 수신, 최소 점수, 결과 0건 알림, 정정 알림을 설정할 수 있다.
- 발송 성공/실패, 제공업체 메시지 ID, 시도 횟수를 기록한다.

### FR-12. 검색, 필터, 정렬 및 저장

- 공고명, 공고번호, 기관명, 분석 텍스트의 통합 검색을 제공한다.
- 날짜, 업무 구분, 점수, 등급, 신뢰도, 예산, 마감 상태, 기관, 참가조건, 신규/정정/재공고, 분석 상태로 필터링한다.
- 기본 정렬은 `적합도 내림차순 → 마감 임박순`이다.
- 사용자는 공고를 `검토 전`, `검토 중`, `참여`, `미참여`, `보관`으로 분류할 수 있다.
- 공고별 개인 메모를 작성할 수 있다.
- 목록을 CSV로 내보낼 수 있으며 원문 파일의 일괄 내보내기는 MVP에서 제외한다.

### FR-13. 피드백 및 품질 개선

- 사용자는 추천 건마다 `관련 있음`, `관련 없음`, `판단 어려움`을 선택할 수 있다.
- `관련 없음` 선택 시 선택 사유(주제 불일치, 자격 불일치, 예산, 일정, 중복, 기타)를 받을 수 있다.
- 피드백은 즉시 과거 점수를 덮어쓰지 않고 별도로 저장한다.
- 운영자는 주제별 정밀도와 오탐 사유를 분석할 수 있다.
- 개인화 학습 또는 자동 가중치 변경은 충분한 피드백과 검증 절차가 마련되기 전에는 적용하지 않는다.

### FR-14. 관리자 운영 기능

- 배치 실행 이력과 유형별 수집/처리 건수를 본다.
- 수집, 다운로드, 추출, OCR, 분석, 알림 단계별 실패 건을 본다.
- 공고 1건 또는 특정 배치를 단계별로 재처리한다.
- API 호출량, LLM 토큰/비용, 저장공간, 이메일 발송량을 본다.
- 분석 모델과 프롬프트/점수 버전을 활성화 또는 롤백한다.
- 사용자별 원문 재분석 요청과 데이터 삭제 요청을 처리한다.

## 9. 데이터 파이프라인

```text
09:00 KST Scheduler
  → 유형별 나라장터 API 수집(72시간 중첩)
  → 원시 응답 저장 및 공통 스키마 정규화
  → 공고/버전 중복 제거 및 변경 감지
  → 구조화 조건·저비용 의미 검색으로 후보 선별
  → 첨부 URL 수집 및 안전한 다운로드
  → 파일 형식별 텍스트 추출/OCR
  → 공고 공통 요약 및 정보 추출
  → 활성 주제별 적합도·참가조건·신뢰도 계산
  → 결과 저장 및 임계값 판정
  → 웹 알림 생성 및 사용자별 이메일 발송
  → 메트릭/감사 로그 기록
```

### 9.1 처리 상태

각 공고 버전은 다음 상태를 가진다.

`DISCOVERED → NORMALIZED → FILTERED_IN → DOWNLOADING → EXTRACTING → ANALYZING → SCORED → NOTIFIED`

오류 시 `PARTIAL` 또는 `FAILED`로 이동하며 마지막 성공 단계부터 재개할 수 있어야 한다. 사전 필터 탈락 건은 `FILTERED_OUT`으로 저장한다.

### 9.2 멱등성 키

- 공고 버전: `bid_ntce_no + bid_ntce_ord`
- 원시 응답: `source + operation + request_window + page_no + response_hash`
- 첨부: `notice_version_id + source_url + content_hash`
- 분석: `notice_version_id + document_set_hash + analysis_version`
- 주제 점수: `analysis_id + topic_version_id + scoring_version`
- 알림: `user_id + topic_score_id + notification_type + event_type`

## 10. 주요 데이터 모델

| 엔터티 | 주요 필드 |
|---|---|
| User | id, email, name, role, timezone, status |
| Organization | id, name, plan, retention_policy |
| Topic | id, organization_id, name, status, current_version_id |
| TopicVersion | id, description, capabilities, include/exclude concepts, filters, weights, threshold, created_at |
| BidNotice | id, bid_ntce_no, source, current_version_id |
| BidNoticeVersion | id, bid_ntce_ord, business_type, title, agencies, dates, amounts, method, detail_url, raw_hash, status |
| Eligibility | notice_version_id, license, industry, region, joint_supply, extracted_conditions |
| Attachment | id, notice_version_id, source_url, original_name, mime, size, sha256, storage_key, status |
| ExtractedDocument | attachment_id, extractor_version, text_storage_key, page_map, quality, status |
| NoticeAnalysis | id, notice_version_id, document_set_hash, summary, structured_json, model, prompt_version, status |
| TopicScore | analysis_id, topic_version_id, score, grade, confidence, eligibility_status, components, reasons, penalties |
| Notification | id, user_id, topic_score_id, channel, event_type, status, sent_at |
| UserBidState | user_id, notice_id, state, memo, feedback, feedback_reason |
| BatchRun | id, scheduled_for, window_start, window_end, status, counts, error_summary |
| AuditLog | actor, action, entity_type, entity_id, metadata, created_at |

## 11. API/응답 필드 매핑 기준

최소한 다음 나라장터 응답 정보를 공통 스키마에 매핑한다. 실제 필드명과 업무 구분별 제공 여부는 개발 착수 시 최신 OpenAPI 참고문서 및 샘플 응답으로 다시 검증한다.

| 목적 | 대표 API 필드 |
|---|---|
| 공고 식별 | `bidNtceNo`, `bidNtceOrd` |
| 공고 정보 | `bidNtceNm`, `ntceKindNm`, `bidNtceDt` |
| 기관 | `ntceInsttCd/Nm`, `dminsttCd/Nm` |
| 일정 | `bidBeginDt`, `bidClseDt`, `opengDt` |
| 금액 | `asignBdgtAmt`, `presmptPrce`, 기초금액 정보 |
| 계약/입찰 | `bidMethdNm`, `cntrctCnclsMthdNm` |
| 상세 링크 | `bidNtceDtlUrl` |
| 변경/재공고 | `reNtceYn`, 공고 변경이력 정보 |
| 첨부 | `ntceSpecDocUrl1~10`, `ntceSpecFileNm1~10`, 보충설명/표준공고 문서 URL |
| 참가 조건 | 면허제한정보, 참가가능지역정보, 공동수급 관련 필드 |

금액은 `배정예산`, `추정가격`, `기초금액`을 서로 다른 필드로 보존하고 UI에서 금액의 종류와 부가세 포함 여부를 명시한다. 임의로 하나의 값으로 합치지 않는다.

## 12. 비기능 요구사항

### NFR-01. 성능 및 처리시간

- 일일 배치는 정상 API/AI 가용성 조건에서 10:30 KST 이전 완료를 목표로 한다.
- API 목록 화면의 p95 응답시간은 캐시 기준 2초 이내다.
- 공고 상세 화면의 p95 응답시간은 3초 이내다.
- 사용자 수와 공고 수 증가에 따라 다운로드·추출·분석 워커를 독립 확장할 수 있어야 한다.

### NFR-02. 가용성과 복구

- 월간 서비스 가용성 목표는 99.5%다(외부 API/AI 장애 제외).
- 모든 비동기 작업은 영속 큐를 사용하고 최소 1회 전달을 전제로 멱등 처리한다.
- 데이터베이스는 일 1회 이상 백업하고 복구 절차를 분기별 검증한다.
- 외부 서비스 장애 시 단계별 재시도 후 재처리 큐에 보존한다.

### NFR-03. 보안

- 전 구간 TLS를 사용한다.
- 비밀번호는 검증된 강한 단방향 해시로 저장한다.
- API 키, LLM 키, 이메일 키는 비밀 관리 시스템에 저장하고 주기적으로 교체할 수 있어야 한다.
- 원문 파일은 비공개 객체 저장소에 두고 짧은 만료시간의 서명 URL로 제공한다.
- 업로드/다운로드 파일은 악성코드 검사와 콘텐츠 형식 검증을 거친다.
- 프롬프트 인젝션 방지를 위해 첨부문서는 명령이 아닌 비신뢰 데이터로 취급하고, 분석 모델이 외부 도구나 비밀정보에 접근하지 못하게 한다.
- 로그에 인증키, 원문 전문, 민감정보를 남기지 않는다.
- 조직 단위 접근제어를 모든 조회에 적용한다.

### NFR-04. 개인정보 및 보존

- 서비스 제공에 필요한 최소 사용자 정보만 수집한다.
- 원문 파일과 분석 데이터의 기본 보존기간은 1년으로 하되 관리자가 정책을 변경할 수 있다.
- 계정 삭제 또는 조직 요청 시 법적 보존 의무가 없는 사용자 데이터와 메모를 정해진 기간 내 삭제한다.
- 외부 AI 제공업체로 전송되는 데이터 범위와 학습 사용 여부를 계약 및 개인정보처리방침에 명시한다.

### NFR-05. 접근성과 사용성

- 키보드만으로 주요 흐름을 사용할 수 있어야 한다.
- 점수는 색상만으로 구분하지 않고 숫자와 등급을 함께 표시한다.
- 날짜는 KST와 절대 날짜/시각을 표시하고 마감 임박은 상대 시간으로 보조한다.
- 모바일에서는 알림과 공고 상세의 핵심 정보 확인을 지원한다.

### NFR-06. 관측 가능성

- 구조화 로그, 메트릭 및 분산 추적 ID를 제공한다.
- 다음 핵심 메트릭을 모니터링한다: API 성공률/지연, 유형별 수집 건수, 중복률, 첨부 다운로드 성공률, 텍스트 추출 성공률, OCR 비율, 분석 성공률/지연/비용, 알림 발송 성공률, 배치 완료 시각.
- 전일 대비 수집량이 비정상적으로 낮거나 0건이면 API 성공 응답이어도 경고한다.

## 13. 오류 및 예외 처리

| 상황 | 제품 동작 |
|---|---|
| OpenAPI 인증/한도 오류 | 배치 중단, 관리자 경고, 키 노출 없는 오류 기록 |
| 일시적 API 오류 | 백오프 재시도 후 실패 페이지부터 재개 |
| 응답 스키마 변경 | 원시 응답 보존, 파싱 실패 격리, 관리자 경고 |
| 첨부 URL 없음 | 메타데이터만 분석하고 신뢰도 하향 |
| 파일 다운로드 실패 | 파일별 재시도, 공고 분석은 부분 진행 |
| HWP/PDF 추출 실패 | OCR/대체 추출 시도 후 `부분 분석` 표시 |
| LLM 응답 형식 오류 | JSON 자동 수정 1회 후 재처리 큐 이동 |
| LLM 장애/한도 초과 | 분석 보류, 수집·다운로드 결과 보존, 복구 후 재개 |
| 이메일 발송 실패 | 채널별 재시도, 웹 알림은 유지 |
| 동일 배치 재실행 | upsert와 멱등키로 중복 공고/알림 방지 |
| 정정공고 발생 | 변경 필드/첨부 재분석 후 중요 변경 알림 |

## 14. 분석 품질 및 안전 원칙

- 앱은 추천과 검토 보조 도구이며 입찰 참가 자격을 확정하지 않는다.
- UI와 이메일에 “원 공고 및 첨부문서를 최종 확인해야 한다”는 안내를 표시한다.
- 근거 없는 요약/점수는 사용자에게 노출하지 않는다.
- 근거는 가능한 한 파일명과 페이지/문단 위치를 포함한다.
- 참가조건이 불명확한 경우 `충족`으로 간주하지 않고 `확인 필요`로 표시한다.
- OCR 품질이나 문서 누락이 점수 신뢰도에 반영되어야 한다.
- 모델 교체 전 고정 평가세트에서 회귀 테스트를 수행한다.

## 15. 성공 지표

### 15.1 제품 KPI

| 지표 | MVP 8주 목표 | 측정 방식 |
|---|---:|---|
| 상위 추천 정밀도(Precision@알림) | 80% 이상 | 사용자의 `관련 있음/없음` 피드백 |
| 일일 배치 정시 완료율 | 95% 이상 | 10:30 KST 이전 완료 배치 비율 |
| 첨부 다운로드 성공률 | 95% 이상 | URL이 존재하는 첨부 중 성공 비율 |
| 지원 형식 텍스트 추출 성공/부분성공률 | 90% 이상 | 지원 파일 중 성공+부분성공 비율 |
| 알림 중복률 | 0.1% 미만 | 동일 멱등키의 중복 발송 비율 |
| 이메일 발송 성공률 | 98% 이상 | 발송 성공/전체 시도 |
| 주간 활성 사용자의 추천 검토율 | 60% 이상 | 추천 상세 열람 또는 상태 변경 비율 |

### 15.2 품질 평가세트

- 실제 관심 주제 3개 이상, 주제당 최소 100개 공고를 사람이 관련/비관련 및 핵심 근거로 라벨링한다.
- 적합도 구간별 정밀도, 재현율, 순위 상관, 근거 정확도를 측정한다.
- 문서 누락, 스캔 PDF, 구형 HWP, 표 중심 문서, 정정공고를 반드시 포함한다.
- 모델/프롬프트/점수식 변경 시 기존 버전과 비교하고 기준 미달 시 배포하지 않는다.

## 16. MVP 범위와 단계별 출시

### Phase 0. 기술 검증(1~2주)

- 운영용 OpenAPI 승인/트래픽 한도 확인
- 업무 구분별 API 샘플과 필드 매핑 검증
- 실제 첨부 URL 다운로드 성공률 및 이용 조건 확인
- PDF/HWP/HWPX/DOCX 추출 품질 검증
- 1일 공고량, 후보 필터 통과율, 평균 첨부 용량 및 LLM 비용 측정
- 수동 라벨 100건으로 점수식 초안 평가

### Phase 1. 내부 MVP(4~6주)

- 인증과 1개 조직
- 주제 CRUD 및 기본 필터
- 매일 09:00 수집·다운로드·추출·분석
- 점수, 요약, 근거, 신뢰도
- 대시보드/목록/상세
- 웹 알림과 이메일
- 배치 모니터링 및 수동 재처리
- 사용자 피드백

### Phase 2. 베타 고도화

- 복수 조직과 초대/권한 강화
- 사용자별 가중치 및 30일 재분석
- 정정공고 문서 diff 고도화
- 알림 채널 확장
- 과거 낙찰/계약 데이터 연계
- 피드백 기반 개인화 및 추천 품질 리포트

## 17. 출시 수용 기준

MVP는 다음 조건을 모두 충족해야 한다.

1. 7일 연속 09:00 배치가 자동 실행되고 동일 구간 재실행 시 중복 데이터/알림이 없다.
2. 물품·용역·공사·외자 샘플이 공통 스키마에 정상 저장된다.
3. 실제 첨부가 있는 테스트 공고에서 원본 다운로드, 해시, 텍스트 추출 및 출처 연결이 동작한다.
4. 지원 파일 형식별 성공·부분성공·실패 상태가 UI에 정확히 표시된다.
5. 주제별 0~100 점수의 구성 합계가 일치하고 근거가 하나 이상 제공된다.
6. 자격 불일치가 적합도 0점으로 오인되지 않고 별도 상태로 표시된다.
7. 임계값 이상 공고만 웹/이메일 알림에 포함되며 건별 상세 링크가 동작한다.
8. 정정공고에서 중요 필드 변경이 감지되고 재분석/변경 알림이 동작한다.
9. API 키와 원문 파일이 클라이언트 또는 애플리케이션 로그에 노출되지 않는다.
10. 관리자 화면에서 각 단계 실패를 확인하고 안전하게 재처리할 수 있다.
11. 고정 평가세트에서 상위 추천 정밀도 80% 이상을 달성한다.
12. 서비스 안내에 AI 분석의 한계와 원문 최종 확인 필요성이 표시된다.

## 18. 테스트 시나리오

### 18.1 핵심 정상 시나리오

- 전일 공고를 유형별로 여러 페이지 수집하고 모든 페이지 수가 합계와 일치한다.
- 동일 공고가 72시간 중첩 구간에 다시 등장해도 1건으로 유지된다.
- 동일 첨부가 재노출되면 해시 캐시를 재사용한다.
- 서로 다른 주제 두 개에 대해 동일 공고가 서로 다른 점수와 근거를 생성한다.
- 임계값 70점 사용자는 69점 공고를 이메일에서 받지 않고 웹 검색에서는 볼 수 있다.
- 정정공고의 마감일 변경을 감지해 변경 알림을 발송한다.

### 18.2 실패/경계 시나리오

- API 429 및 5xx 후 재시도가 성공한다.
- 한 유형의 마지막 페이지에서 빈 목록이 반환되어도 무한 반복하지 않는다.
- 공고명은 관련 없어 보이나 첨부 본문은 높은 관련성이 있는 공고를 의미 검색이 찾는다.
- 제외 키워드가 단순 참고문헌에만 등장할 때 강제 제외되지 않는다.
- ZIP 안에 경로 이탈 파일이나 과도하게 큰 압축 데이터가 있을 때 격리한다.
- OCR 품질이 낮을 때 높은 신뢰도로 표시하지 않는다.
- 프롬프트 인젝션 문구가 문서에 있어도 시스템 동작이나 점수 규칙을 바꾸지 못한다.
- 이메일 재시도 중 동일 공고가 중복 발송되지 않는다.
- KST 자정과 서머타임 없는 환경에서 조회 경계가 정확하다.

## 19. 리스크와 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| OpenAPI 한도/정책/스키마 변경 | 수집 중단 또는 누락 | 어댑터 계층, 원시 응답 보존, 호출량 모니터링, 운영 한도 사전 증설 |
| 첨부 URL 다운로드 제약 | 핵심 본문 분석 실패 | 기술 검증에서 성공률 측정, 공식 제공 URL 우선, 실패 시 메타 분석과 명확한 상태 표시 |
| HWP/스캔 문서 추출 품질 | 부정확한 요약과 점수 | 다중 추출기, OCR, 품질 점수, 원문 링크, 부분 분석 처리 |
| 일일 공고량에 따른 AI 비용 | 비용 급증·지연 | 저비용 후보 필터, 추출/요약 캐시, 배치 큐, 예산 한도와 비용 경고 |
| LLM 환각/근거 오류 | 사용자 신뢰 저하 | 출처 강제, 스키마 검증, 불확실성 표기, 평가세트와 회귀 테스트 |
| 자격 판단 오해 | 잘못된 입찰 의사결정 | 적합도와 자격 상태 분리, 확정 표현 금지, 원문 최종 확인 고지 |
| 정정/재공고 중복 | 알림 피로·기회 누락 | 공고번호/차수 버전 관리, 변경 diff, 이벤트별 멱등키 |
| 악성 첨부/프롬프트 인젝션 | 보안 사고 | 격리 다운로드, 악성코드/압축 검사, 비신뢰 콘텐츠 처리, 비밀/도구 접근 차단 |

## 20. 결정이 필요한 오픈 이슈

아래 항목은 Phase 0에서 확인 후 확정한다.

1. 조직별 사용자인지 단일 개인용으로 시작할지
2. 초기 핵심 업무 구분을 용역 중심으로 제한할지, 4개 유형을 동시에 제공할지
3. 이메일 외에 최초로 추가할 알림 채널
4. 사용자가 보유 자격/면허를 구조화해 등록할 범위
5. 과거 공고 재분석 기간과 요금/비용 정책
6. 원문 파일과 분석 결과의 최종 보존기간
7. 외부 LLM 사용 여부, 데이터 리전 및 비학습 계약 조건
8. 첨부 URL의 자동 다운로드가 최신 이용 조건에 부합하는지에 대한 운영 검토
9. 사용자별 가중치 조정을 MVP에 노출할지 관리자 설정으로 제한할지

## 21. 외부 의존성

- 공공데이터포털 계정과 조달청 나라장터 입찰공고정보서비스 활용 승인/인증키
- 운영 호출량에 맞는 OpenAPI 트래픽 한도
- 원본 파일용 비공개 객체 저장소
- PDF/HWP/HWPX/Office 문서 추출기 및 한국어 OCR
- 임베딩/LLM 제공업체 또는 자체 모델 인프라
- 이메일 발송 서비스
- 스케줄러, 영속 작업 큐, 관계형 데이터베이스, 검색 인덱스
- 오류 모니터링 및 로그/메트릭 시스템

## 22. 공식 참고자료

- [공공데이터포털: 조달청_나라장터 입찰공고정보서비스](https://www.data.go.kr/data/15129394/openapi.do) — REST, JSON/XML, 물품·용역·공사·외자별 목록/상세 및 기초금액·면허제한·참가지역·변경이력 제공 범위와 트래픽 정책 확인
- [공공데이터포털 공지: 입찰공고정보서비스 24시간 운영](https://www.data.go.kr/bbs/ntc/selectNotice.do?originId=NOTICE_0000000003938) — 2025-02-04 기준 입찰공고정보서비스 25개 오퍼레이션의 24시간 개방 및 과도한 호출 시 제한 가능성 확인
- 공공데이터포털 상세 페이지의 최신 `조달청_OpenAPI참고자료_나라장터_입찰공고정보서비스` 문서 — 개발 착수 시 오퍼레이션별 필드, 코드, 날짜 형식 및 샘플 응답을 재확인할 것

---

## 부록 A. 기본 이메일 예시

**제목:** `[BidFit] 8월 24일 신규 추천 7건 · 매우 높음 2건`

**요약:**

- 분석 공고: 1,284건
- 주제 후보: 31건
- 알림 기준 통과: 7건
- 정정 중요 변경: 1건
- 부분 분석: 2건

**공고별 블록:**

> **87점 · 매우 높음 · 신뢰도 높음**  
> 생성형 AI 기반 민원업무 지원 시스템 구축  
> 수요기관: 예시기관 · 예산: 4.2억원 · 마감: 2026-08-31 10:00  
> 적합 이유: 생성형 AI, 지식검색, 내부업무 자동화가 핵심 과업으로 명시됨  
> 참가조건: 확인 필요 — 소프트웨어사업자 신고 조건 확인 필요  
> [상세 분석 보기] [나라장터 원문 보기]

## 부록 B. 용어 정의

| 용어 | 정의 |
|---|---|
| 적합도 | 사용자가 정의한 관심 주제와 공고 과업의 부합 정도 |
| 참가조건 상태 | 면허, 업종, 지역, 실적 등 실제 참여 가능성에 대한 보조 판단 |
| 신뢰도 | 문서 확보/추출 품질과 직접 근거의 충분성 |
| 공고 버전 | 동일 입찰공고번호의 특정 공고차수 및 변경 상태 |
| 부분 분석 | 첨부 일부 누락/추출 실패 등으로 제한된 정보만 사용한 분석 |
| 중요 변경 | 마감, 예산, 참가 자격, 과업 범위, 첨부문서 등 의사결정에 영향을 주는 변경 |
